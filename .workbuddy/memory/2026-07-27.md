# 2026-07-27

- 重写 `dev/DevHandbook.html`，基于当前源码重新整理 MediaSync 桌面应用的架构、模块边界、启动链路、配置契约、索引与缓存、QC 引擎、线程模型、FFmpeg 集成、构建发布、测试验证、修改指南、常见陷阱和 ADR。
- 明确当前项目为 PySide6 模块化桌面单体，ProjectSync 与 VideoQC 在同一进程内集成；运行时主配置位于 `%APPDATA%/MediaSync/config.json`；分发形态为 PyInstaller `onedir` + Inno Setup。
- 校验新文档包含 16 个章节锚点且 HTML 尾部闭合，已可作为后续维护开发手册使用。
- 同步重写 `README.md`，修正旧配置路径、单文件分发等过时描述，统一到当前 `MediaSync` / `%APPDATA%/MediaSync` / PyInstaller `onedir` 架构认知。
- 新增 `dev/DevHandbook.md` 作为仓库内可维护的 Markdown 版开发手册，保留项目身份、架构决策、模块边界、数据契约、线程并发、构建测试、修改指南与 ADR 摘要。
- 修复 `installer/MediaSync-Setup.iss` 的升级安装识别逻辑：不再用应用名猜测卸载键，改为基于固定 AppId 检测既有安装，并在欢迎页、准备安装页、完成页明确提示“这是更新安装，配置/索引/QC 缓存会保留���。
- 重新编译安装器并确认 `dist/installer/MediaSync-Setup.exe` 生成成功；同时修正 `installer/build-installer.bat` 中把 PyInstaller 产物误写为 single-file 的过时注释。
